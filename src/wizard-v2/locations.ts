const baseUrl = '/wizard-navigation-v2';

const locations = {
  base: baseUrl,
  name: `${baseUrl}/name`,
  sex: `${baseUrl}/sex`,
  mobileNumber: `${baseUrl}/mobile-number`,
}
export default locations;